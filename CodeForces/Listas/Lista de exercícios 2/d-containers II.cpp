#include <bits/stdc++.h>

// ios::sync_with_stdio(false);
// cin.tie(0);
// cout.tie(0);
// typedef ORIGINAL NEW;

using namespace std;

int main(){
  // Estrutura
  queue<int> containers;
  // Entrada
  int aux;
  while(scanf("%c",&aux) != -1){
    aux -= 65;
    if(aux >= 0 && aux <= 25)
      containers.push(aux);  
  }
  // Processamento
  
  return 0; // \n
}
